# MVP Scope Definition Template

> Fill in all fields before activating the `mvp-scope-definition` skill.

---

## Product Overview

**Product:** `{{product-name}}`

**Problem:** {{the core problem being solved}}

**Target user:** {{who the MVP is for}}

**MVP goal:** {{what you want to learn or prove with the MVP}}

---

## Core User Journey (MVP must support this)

```
1. {{e.g. User signs up}}
2. {{e.g. User creates first project}}
3. {{e.g. User invites a teammate}}
4. {{e.g. User completes core task}}
5. {{e.g. User sees value / outcome}}
```

---

## Feature Scope

### In MVP (must have)

| Feature | Why it's in MVP | Effort |
|---------|----------------|--------|
| `{{feature}}` | {{e.g. core value prop, can't validate without it}} | {{S/M/L}} |
| `{{feature}}` | {{e.g. required for user to complete journey}} | {{S/M/L}} |

### Not in MVP (explicitly excluded)

| Feature | Why excluded | When to add |
|---------|-------------|-------------|
| `{{feature}}` | {{e.g. nice-to-have, not core to validation}} | {{e.g. v2 if retention is good}} |
| `{{feature}}` | {{e.g. complex, can be manual for now}} | {{e.g. after 100 users}} |

---

## MVP Success Metrics

| Metric | Target | Measurement |
|--------|--------|-------------|
| {{e.g. Activation rate}} | {{e.g. ≥ 40%}} | {{e.g. % users who complete core journey}} |
| {{e.g. Retention (week 1)}} | {{e.g. ≥ 30%}} | {{e.g. % users who return after 7 days}} |
| {{e.g. Paying customers}} | {{e.g. 10}} | {{e.g. within 30 days of launch}} |

---

## Timeline

| Phase | Duration | Deliverable |
|-------|----------|-------------|
| Design | {{e.g. 1 week}} | {{wireframes, user flows}} |
| Build | {{e.g. 4 weeks}} | {{working MVP}} |
| Test (internal) | {{e.g. 1 week}} | {{bug fixes, polish}} |
| Launch | {{e.g. week 6}} | {{first 10 users}} |

**Total:** {{e.g. 6 weeks}}
