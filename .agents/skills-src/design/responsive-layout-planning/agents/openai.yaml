interface:
  display_name: "responsive-layout-planning"
  short_description: "Plan how screen layouts adapt across breakpoints (mobile/tablet/desktop). Produces a structured reflow spec for CSS Grid/Flexbox or Figma. Not for high-fidelity visual design."

policy:
  allow_implicit_invocation: true
