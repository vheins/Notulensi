display_name: "start"
short_description: "Use at the beginning of every session to load rules, verify context, and prepare the quality gate before tasks. Always run this first."
allow_implicit_invocation: true
