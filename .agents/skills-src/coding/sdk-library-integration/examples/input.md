# Example Input: sdk-library-integration

### Example 1: TypeScript + Stripe SDK
**Input:**
- `{{tech_stack}}`: TypeScript + Node.js + Express
- `{{sdk_name}}`: Stripe Node.js SDK v14
- `{{integration_requirements}}`: "Create payment intents, confirm payments, handle webhook events. Retry on network errors. Mock for tests."


### Example 2: Python + AWS S3 SDK (boto3)
**Input:**
- `{{tech_stack}}`: Python + FastAPI
- `{{sdk_name}}`: AWS SDK for Python (boto3) v1.34 — S3 client
- `{{integration_requirements}}`: "Upload files, generate presigned URLs, delete files. Handle S3 errors. Mock for tests."
