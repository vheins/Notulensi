# Example Input: boilerplate-generation

### Example 1: Node.js + TypeScript + Express REST API

**Input:**
- `{{tech_stack}}`: Node.js 20 + TypeScript 5 + Express + Prisma + PostgreSQL
- `{{project_type}}`: REST API
- `{{features}}`: "JWT authentication, database, structured logging (pino), Docker, Jest testing"

---

### Example 2: Python + FastAPI + SQLAlchemy

**Input:**
- `{{tech_stack}}`: Python 3.11 + FastAPI + SQLAlchemy 2 + Alembic + PostgreSQL
- `{{project_type}}`: REST API microservice
- `{{features}}`: "database migrations, structured logging, Docker, pytest testing, CORS"
