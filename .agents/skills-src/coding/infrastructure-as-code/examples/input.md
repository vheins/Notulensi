# Example Input: infrastructure-as-code

### Example 1: Terraform — AWS ECS + RDS + ALB
**Input:**
- `{{tech_stack}}`: AWS + Node.js containerized API
- `{{iac_tool}}`: Terraform
- `{{infrastructure_requirements}}`: "ECS Fargate cluster, RDS PostgreSQL (Multi-AZ), Application Load Balancer, VPC with public/private subnets, ECR repository, IAM roles"


### Example 2: AWS CDK (TypeScript) — Lambda + API Gateway + DynamoDB
**Input:**
- `{{tech_stack}}`: AWS + TypeScript serverless
- `{{iac_tool}}`: AWS CDK (TypeScript)
- `{{infrastructure_requirements}}`: "Lambda function, API Gateway REST API, DynamoDB table, CloudWatch alarms, IAM role with least privilege"
