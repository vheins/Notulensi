# Example Input: environment-setup

### Example 1: Node.js + TypeScript + PostgreSQL + Redis
**Input:**
- `{{tech_stack}}`: Node.js 20 + TypeScript + Express + PostgreSQL 15 + Redis 7
- `{{project_description}}`: "E-commerce API with user auth, product catalog, and order management. Uses PostgreSQL for data and Redis for session caching."
- `{{os_target}}`: "macOS + Linux (Ubuntu/Debian)"


### Example 2: Python + FastAPI + MySQL + Docker
**Input:**
- `{{tech_stack}}`: Python 3.11 + FastAPI + SQLAlchemy + MySQL 8 + Docker
- `{{project_description}}`: "REST API for a SaaS analytics platform. Uses MySQL for relational data, Docker for local dev parity with production."
- `{{os_target}}`: "macOS + Windows (WSL2)"
