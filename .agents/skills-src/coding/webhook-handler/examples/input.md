# Example Input: webhook-handler

### Example 1: Node.js + Express + Stripe Webhooks
**Input:**
- `{{tech_stack}}`: Node.js + TypeScript + Express + PostgreSQL + Redis
- `{{webhook_provider}}`: Stripe
- `{{event_types}}`: "payment_intent.succeeded, payment_intent.payment_failed, customer.subscription.deleted"


### Example 2: Python + FastAPI + GitHub Webhooks
**Input:**
- `{{tech_stack}}`: Python + FastAPI + PostgreSQL + Redis
- `{{webhook_provider}}`: GitHub
- `{{event_types}}`: "push, pull_request.opened, pull_request.closed, check_run.completed"
