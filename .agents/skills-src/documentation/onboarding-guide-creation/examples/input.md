# Example Input: onboarding-guide-creation

### Example 1: Node.js Microservice Team
**Input:**
- `{{project_name}}`: "notifications-service"
- `{{tech_stack}}`: "Node.js 20, Express 4, PostgreSQL 15, Redis 7, Docker, GitHub Actions"
- `{{team_name}}`: "Platform Engineering"
- `{{repo_url}}`: "https://github.com/acme/notifications-service"
- `{{first_task_description}}`: "Add an `updated_at` field to the GET /notifications/:id response payload"


### Example 2: Go + Kubernetes — Backend Platform Team
**Input:**
- `{{tech_stack}}`: Go + Kubernetes + Helm + PostgreSQL + Kafka
- `{{project_name}}`: "PlatformCore"
- `{{team_context}}`: "Infrastructure team of 8. New engineers join quarterly. Services deployed to GKE."
