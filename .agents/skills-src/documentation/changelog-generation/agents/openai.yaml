interface:
  display_name: "changelog-generation"
  short_description: "Generate a CHANGELOG.md following the Keep a Changelog format from git logs or lists of changes. Do NOT use for release notes in a different format, API docs, or commit message generation."

policy:
  allow_implicit_invocation: true
