# Example Input: sprint-planning

### Example 1: React + Node.js — SaaS Product Sprint
**Input:**
- `{{backlog_items}}`: "1) User profile page (edit name, avatar, email), 2) Password reset flow, 3) Fix: login page flickers on mobile, 4) Add rate limiting to auth endpoints, 5) Refactor user service to use repository pattern, 6) Email notification for new team member invite, 7) Spike: evaluate WebSocket vs. SSE for real-time updates"
- `{{sprint_duration}}`: "2 weeks"
- `{{team_velocity}}`: "36 points/sprint, 2 backend engineers, 1 frontend engineer, 1 QA"


### Example 2: Python + Django — Data Platform Sprint
**Input:**
- `{{backlog_items}}`: "1) CSV import endpoint for bulk data upload, 2) Data validation pipeline with error reporting, 3) Fix: export endpoint returns 500 on empty dataset, 4) Add pagination to data listing API, 5) Write integration tests for import pipeline, 6) Performance: optimize slow query on data search endpoint"
- `{{sprint_duration}}`: "1 week"
- `{{team_velocity}}`: "20 points/sprint, 2 backend engineers, 1 QA"
