# Example Input: microservices-boundary-definition

### Example 1: Node.js + Kafka + Kubernetes — E-commerce Platform
**Input:**
- `{{domain_description}}`: "E-commerce platform with product catalog, order management, payment processing, inventory tracking, customer accounts, and shipping/fulfillment."
- `{{tech_stack}}`: "Node.js + PostgreSQL + Kafka + Kubernetes + REST + gRPC"
- `{{team_structure}}`: "3 teams: Team A (catalog + inventory), Team B (orders + payments), Team C (customers + shipping)"


### Example 2: Python + FastAPI + RabbitMQ — SaaS Platform
**Input:**
- `{{domain_description}}`: "B2B SaaS with user management, project management, billing/subscriptions, notifications, and reporting/analytics."
- `{{tech_stack}}`: "Python + FastAPI + PostgreSQL + RabbitMQ + Docker"
- `{{team_structure}}`: "2 teams: Team A (product features: users, projects), Team B (platform: billing, notifications, analytics)"
