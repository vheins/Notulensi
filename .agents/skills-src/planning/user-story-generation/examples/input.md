# Example Input: user-story-generation

### Example 1: React + Node.js — Notification System
**Input:**
- `{{feature_name}}`: "In-App Notification Center"
- `{{user_role}}`: "registered user"
- `{{context}}`: "SaaS project management tool. Users receive notifications for task assignments, mentions, and deadline reminders. Notifications should be dismissible and show unread count."


### Example 2: Django + Vue.js — File Upload
**Input:**
- `{{feature_name}}`: "Document Upload and Management"
- `{{user_role}}`: "project member"
- `{{context}}`: "B2B collaboration platform. Members upload PDFs and images to project workspaces. Max file size 50MB. Files should be previewable and deletable by the uploader."
