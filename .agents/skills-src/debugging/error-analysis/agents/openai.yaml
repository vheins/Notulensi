interface:
  display_name: "error-analysis"
  short_description: "Identify root cause, affected component, and concrete fix from error messages and stack traces. Do NOT use for performance profiling, memory leak detection, or architectural reviews."

policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "@modelcontextprotocol/server-sequential-thinking"
      description: "Structured multi-step reasoning for complex problem solving"
    - type: "mcp"
      value: "@upstash/context7-mcp"
      description: "Up-to-date library documentation and code examples"
