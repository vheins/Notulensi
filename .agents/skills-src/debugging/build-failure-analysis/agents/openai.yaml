interface:
  display_name: "build-failure-analysis"
  short_description: "Analyze build failures to identify root causes and provide fix instructions. Do not use for deployment failures or runtime errors."

policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "@modelcontextprotocol/server-sequential-thinking"
      description: "Structured multi-step reasoning for complex problem solving"
    - type: "mcp"
      value: "@upstash/context7-mcp"
      description: "Up-to-date library documentation and code examples"
