interface:
  display_name: "canary-deployment-test-plan"
  short_description: "Designs a canary deployment test plan with traffic split strategy and rollback criteria. Do NOT use for blue-green deployments or feature flag testing."

policy:
  allow_implicit_invocation: true
