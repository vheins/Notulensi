# Example Input: test-pyramid-strategy

## Example 1: SaaS startup with no test strategy

| Variable | Value |
|----------|-------|
| `{{application_description}}` | B2B SaaS: Node.js API + React frontend + PostgreSQL. Team of 4 devs. Currently no tests. CI takes 0 minutes (no tests). Deploying manually, bugs found in production. |
| `{{tech_stack}}` | Node.js + Express + React + PostgreSQL + Jest + Playwright |
| `{{current_testing}}` | No tests. Manual QA before each deploy. Takes 2 hours. Bugs still reach production. |
