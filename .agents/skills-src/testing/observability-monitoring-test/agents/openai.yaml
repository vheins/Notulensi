interface:
  display_name: "observability-monitoring-test"
  short_description: "Generates tests verifying metrics, logs, and traces are correctly emitted to validate observability setup. Do NOT use for functional or performance tests."

policy:
  allow_implicit_invocation: true
