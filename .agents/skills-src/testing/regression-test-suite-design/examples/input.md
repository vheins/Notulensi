# Example Input: regression-test-suite-design

## Example 1: E-commerce platform

| Variable | Value |
|----------|-------|
| `{{application_description}}` | B2C e-commerce: product catalog, cart, checkout, order management, user auth. Node.js + React + PostgreSQL. |
| `{{tech_stack}}` | Node.js + Jest + supertest + Playwright |
| `{{bug_history}}` | BUG-101: order total wrong with multiple discounts. BUG-102: user could checkout with empty cart. BUG-103: password reset link expired immediately on some timezones. |
