# Scripts: api-contract-test-generation

Folder ini berisi skrip executable yang digunakan oleh skill `api-contract-test-generation`.

## Scripts

| Script | Language | Description | Usage |
|--------|----------|-------------|-------|
| `run-tests.sh` | Bash | Run API contract test suite for this skill | `bash scripts/run-tests.sh` |

## Prerequisites

- bash 4+
- {{test_framework}} installed

## Customization

Ganti semua `{{variable_name}}` dengan nilai aktual sebelum menjalankan skrip.
